 1. 先修事件接口契约（P0）

     关键文件：
     - frontend/src/api/index.js
     - frontend/src/views/ExecutionEvents.vue
     - backend/routers/audits.py
     - frontend/src/api/index.spec.js

     实施：
     - 修正 ExecutionEvents.vue 的 loadEvents：不再调用 getAuditEvents(props.id, params)，改为 getAuditEvents(props.id, afterId, 500)；读取后端返回的 events，不再读取 items。
     - 为避免以后再次误用，在 frontend/src/api/index.js 中让 getAuditEvents 兼容两种调用方式：
       - getAuditEvents(id, afterId, limit)
       - getAuditEvents(id, { after_id, since_sequence, limit })
     并统一映射为后端 query：after_id / limit。
     - 优化 backend/routers/audits.py:get_audit_events 的返回游标：after_id 返回本次响应中最新事件 id（没有新事件时返回请求的 after_id），而不是仅回显入参，便于前端可靠增量拉取。
     - 新增/补充 frontend/src/api/index.spec.js 用例，覆盖数字游标和对象参数两种序列化方式。

     复用：
     - 后端继续复用 backend/services/audit_runtime.py:list_events 与 serialize_event。
     - 前端继续复用现有 RunActivityStream.vue 的事件去重思路（按 event id 去重）。

     2. 修复模型测试诊断与探测行为（P3）

     关键文件：
     - backend/main.py
     - backend/routers/llm_configs.py
     - backend/services/llm_client.py
     - frontend/src/views/LlmConfigs.vue

     实施：
     - 在 backend/main.py 的 HTTPException 统一处理器中，保留 dict detail 的所有诊断字段：将 code / message / details 之外的平铺字段合并进统一错误响应的 details，避免 attempts、preferred_mode、successful_mode 等被丢弃。
     - 在 LlmConfigs.vue 中优先读取 e.details / response.data.details，再兼容旧的 response.data.detail；同时让 normalizeTestResult 兼容 payload.detail 作为 message fallback。
     - 在 backend/services/llm_client.py:_run_test_probe 中放宽 Chat Completions 探测参数：temperature 使用配置值（空值回退 0.1），max_tokens 提升到 128。
     - 在 test_llm_connection 中为每个 API mode 增加一次轻量重试，复用现有错误格式化/诊断函数，并参考正式调用已有的重试退避工具，避免偶发网络抖动造成误判。
     - 保持当前 OpenAI-compatible AsyncOpenAI 客户端实现，不引入 Anthropic SDK 或 Claude 专有请求形态。

     复用：
     - llm_client._format_exception_message
     - llm_client._build_attempt_from_exception
     - 现有正式调用重试相关 helper（如 _retry_delay_seconds/配置项，若当前函数可直接复用）。

     3. 修复 Agent 预算裁剪与跳过展示（P2）

     关键文件：
     - backend/services/supervisor.py
     - backend/tests/test_deterministic_planner.py
     - frontend/src/components/StageProgress.vue
     - frontend/src/components/audit/StageTimeline.vue

     实施：
     - 将 FALLBACK_MAX_AGENT_COUNT 从 7 提升到 9。阶段 1 是架构阶段，子审计阶段最多是 2-9 共 8 个；9 的上限可避免 baseline 2/7/9 挤掉正常业务审计阶段。
     - 调整 _trim_agent_plan_to_budget：当确实需要裁剪时，baseline 仍强制保留，非 baseline 按证据/优先级排序保留，而不是按插入顺序或阶段号裁剪；无法取得证据分时使用现有 priority 作为后备排序。
     - 保留现有 skipped_agents →AuditStage.status='skipped' →stage.findings.skip_reason 的链路；必要时把预算裁剪原因写得更明确（包含 max agent 数和 baseline 保留信息）。
     - 轻量增强 StageProgress.vue：审计阶段 subLabel 区分计划数、执行/完成数、跳过数；StageTimeline.vue 已显示 skip_reason，只需确保文案清晰，不重复造状态来源。
     - 在 backend/tests/test_deterministic_planner.py 增加预算为 9、证据命中 3/4/5/6/8 且 baseline 必保的用例；增加预算不足时高优先级/高证据阶段优先保留的用例。

     复用：
     - supervisor.BASELINE_AGENT_GUIDANCE
     - supervisor._select_fallback_stage_nums 的证据排序思路
     - StageTimeline.vue 已有 skipped 状态与 skip_reason 展示

     4. 修复并发 current_stage 竞争，改以 phase/stages 展示真实进度（P1）

     关键文件：
     - backend/services/supervisor.py
     - backend/services/audit_runtime.py
     - frontend/src/composables/useAuditDerived.js
     - frontend/src/views/AuditDetail.vue
     - frontend/src/utils/auditEvents.js

     实施：
     - 删除 backend/services/supervisor.py:_execute_sub_agents 中并发子 Agent 对 agent_task.current_stage = stage_num 的写入。子 Agent 只更新自己的 AuditStage.status/started_at/completed_at/error_message。
     - 不做数据库迁移，不新增 AuditTask.phase 列；继续复用现有 _set_task_phase 写入的 task.summary.current_phase 与 multi_agent_phase_mode。
     - 确保主流程只在单线程/主阶段边界更新整体阶段信息；前端以 summary.current_phase 和 AuditStage[] 为主展示，不把 current_stage 当作并发子阶段的真实顺序。
     - 在 audit_runtime.py 增加清晰的复核/重跑事件常量（如 review.started、review.completed、rerun.requested、stage.reset_for_rerun），并在 supervisor.py 的复核与 rerun 分支发射事件。
     - 在 frontend/src/utils/auditEvents.js 增加这些新事件的中文标签与摘要，使事件流能解释“复核触发重跑”，避免用户误判为乱序。
     - 复核触发 rerun 后，保留并确认现有 _reload_task_stages(session, task.id) 调用路径，确保重置后的阶段状态被重新加载。

     复用：
     - supervisor._set_task_phase
     - supervisor._reload_task_stages
     - supervisor._reset_agent_stages_for_rerun
     - audit_runtime.emit_event
     - useAuditDerived.currentPhase
     - StageProgress.vue / StageTimeline.vue 的阶段数组展示

     Verification

     实现后建议按以下顺序验证：

     1. 前端事件契约：
       - 运行前端相关单测，至少覆盖 frontend/src/api/index.spec.js。
       - 打开审计事件页面，确认请求 URL 不再出现 after_id=[object Object]。
       - 确认增量刷新不会重复刷全量事件，事件列表能随运行更新。
     2. 模型连接测试：
       - 对一个故意失败的模型配置点击“测试连接”，确认弹窗能显示attempts、preferred_mode、失败诊断。
       - 对一个返回非严格 pong 但有内容的兼容模型，确认基础连通可以被识别。
     3. Planner / Supervisor：
       - 运行 backend/tests/test_deterministic_planner.py，确认 baseline 必保、预算 9、裁剪排序行为正确。
       - 运行一次真实/模拟多 Agent 审计，确认阶段 2-9 最多 8 个子审计可以执行，跳过阶段有明确原因。
     4. 阶段顺序与事件流：
       - 运行多 Agent 审计时观察 current_stage 不再由并发子 Agent 来回覆盖。
       - 观察 StageProgress / StageTimeline 以 phase + stage status 展示进度。
       - 若复核触发 rerun，事件流应出现复核/重跑相关事件，且重跑阶段状态刷新正确。
     5. 回归：
       - 后端：优先跑与本次相关的 backend/tests/test_deterministic_planner.py、backend/tests/test_llm_client.py、backend/tests/test_audit_events.py、backend/tests/test_sse_events.py；时间允许再跑全量 backend/tests。
       - 前端：优先跑
     frontend/src/api/index.spec.js、frontend/src/composables/useAuditDerived.spec.js、frontend/src/utils/auditEvents.spec.js、frontend/src/components/audit/RunActivityStream.spec.js；如新增组件测试一并运行。