# AISCAN 对比优化路线图

本文件记录 `D:\111\codescan` 对比 `D:\111\AISCAN` 后仍值得补齐的优化项。优先级按“用户排障收益、对审计准确性影响、实现风险”排序。

## P0 阶段矩阵工作台

目标：在审计详情页提供紧凑的阶段矩阵，把每个阶段的计划状态、执行状态、Agent 尝试、漏洞数、路由覆盖和错误原因集中展示。

价值：
- 阶段三并行审计是否真正执行、哪些阶段失败或未收敛，可以一眼定位。
- 与现有 `RunDiagnosticsPanel`、`agent_runs`、`diagnostics.orchestration_guard` 形成闭环。
- 前端排障体验接近 AISCAN 的编排工作台。

验收：
- 展示 Stage 1、Stage 2-9、规划、复核的状态。
- 对计划阶段、完成阶段、失败阶段、未收敛阶段有明确标记。
- 显示每阶段 Agent 尝试次数和最近错误。
- 布局紧凑，移动端不挤压、不新增页面级大卡片。

## P1 单阶段补扫和重验

目标：允许用户对单个阶段执行补扫、漏洞重验，避免每次都重跑完整任务。

价值：
- 对误报、漏报和模型偶发失败更易修复。
- 与 AISCAN 的 `gap-check` / `revalidate` 思路对齐。

验收：
- 支持选择一个或多个阶段重跑。
- 支持只重验已有漏洞，不重新扫描源码。
- 重跑后复核状态自动标记需要刷新。

## P1 模型结构化查询工具增强

目标：进一步减少大 JSON 塞进 prompt，提供分页查询路由、漏洞、阶段产物和项目 manifest 的工具。

价值：
- 降低输出截断和上下文污染。
- 大项目审计更稳定。

验收：
- 可分页查询路由清单。
- 可分页查询阶段发现。
- 可查询项目热点文件、入口、规则命中摘要。

## P2 项目 Manifest 一等化

目标：把 `pre_discovery`、规则命中、静态路由、source-sink 线索整合成稳定 manifest。

价值：
- Planner 和子 Agent 都能围绕同一个轻量事实源工作。
- 前端可以展示项目审计边界和热点。

验收：
- 后端生成结构化 manifest。
- 前端显示模块、入口、热点文件、规则命中分布。
- Agent prompt 优先引用 manifest 摘要。

## P2 报告加入运行质量信息

目标：报告不仅列漏洞，也说明本次审计过程质量。

价值：
- 交付时能解释哪些阶段执行完整、哪些阶段降级或失败。
- 避免“有报告但审计未充分完成”的误判。

验收：
- 报告包含阶段执行矩阵。
- 报告包含编排守卫、重跑、失败、跳过摘要。
- 报告包含路由覆盖和复核状态摘要。

## P3 组织和权限隔离

目标：补齐多用户场景下的组织、项目权限和审计任务权限。

价值：
- 更适合多人或团队部署。

验收：
- 项目归属组织。
- 用户按组织授权访问。
- 任务、报告、漏洞接口均校验权限。

## P3 配置严格校验和运行恢复

目标：启动时明确拒绝未知/废弃配置，并提升 Worker 异常恢复能力。

价值：
- 降低生产部署的隐性配置错误。
- 重启后更容易恢复和定位卡住任务。

验收：
- 未知配置字段给出明确错误。
- Worker 崩溃后 running 阶段可被恢复或标记失败。
- 长时间无事件进展会在诊断中稳定呈现 stalled。
